import kivvy
from forms import *

# this should enable the running of a form in a separate thread
# which hooks back on lambda executions.
# enabling logicdb queries as lambdas.
# [form keyword value keyword value keywoard value children values]
# ]